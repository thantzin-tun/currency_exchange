import React from "react";
// import Slider from "react-slick";

type SliderProps = {
  settings: any;
  children: React.ReactNode;
};

export const SliderCom: React.FC<SliderProps> = ({ settings, children }) => {
  return (
    <></>
    // <>
    //   <Slider {...settings}>{children}</Slider>
    // </>
  );
};
